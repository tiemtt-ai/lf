<div class="admin-form-grid"><label>{{ __('lf.LF_learning_definition') }}@if($editing)<input type="hidden" name="node_definition_id" value="{{ $node->node_definition_id }}"><strong>{{ $definitions->firstWhere('id', $node->node_definition_id)?->canonical_name ?? $node->name_snapshot }}</strong>@else<select name="node_definition_id" required>@foreach($definitions->where('status', 'active') as $definition)<option value="{{ $definition->id }}">{{ $definition->canonical_name }} ({{ $definition->node_type }})</option>@endforeach</select>@endif</label><label>{{ __('lf.LF_learning_sequence') }}<input type="number" min="1" name="sequence" value="{{ $node->sequence ?? 1 }}"></label></div><label>{{ __('lf.LF_learning_criteria_json') }}<textarea name="criteria_json">{{ isset($node) ? json_encode(json_decode($node->criteria_snapshot ?? 'null', true), JSON_PRETTY_PRINT|JSON_UNESCAPED_UNICODE) : '' }}</textarea></label>

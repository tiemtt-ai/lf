<div class="admin-form-grid"><label>{{ __('lf.LF_learning_code') }}<input name="code" required value="{{ $definition->code ?? '' }}" @disabled($identityLocked)></label><label>{{ __('lf.LF_learning_type') }}<select name="node_type" @disabled($identityLocked)>@foreach(['objective','concept','competency'] as $type)<option value="{{ $type }}" @selected(($definition->node_type ?? '') === $type)>{{ $type }}</option>@endforeach</select></label><label>{{ __('lf.LF_learning_name') }}<input name="canonical_name" required value="{{ $definition->canonical_name ?? '' }}" @disabled($identityLocked)></label></div>@if($identityLocked)<input type="hidden" name="code" value="{{ $definition->code }}"><input type="hidden" name="node_type" value="{{ $definition->node_type }}"><input type="hidden" name="canonical_name" value="{{ $definition->canonical_name }}"><p>{{ __('lf.LF_learning_definition_identity_locked') }}</p>@endif<label>{{ __('lf.LF_learning_description') }}<textarea name="description">{{ $definition->description ?? '' }}</textarea></label>
